
<script>
	import Layout_MDSVEX_DEFAULT from '/builds/ux-engineering/metal/documentation/src/lib/components/docs/markdown.svelte';
</script>

<Layout_MDSVEX_DEFAULT {...$$props}>
<h1 id="metal-design-tokens-for-components">Metal Design Tokens for Components</h1>
<p>This folder holds the configuration and settings for generating component-specific design tokens for rapid use in documentation as well as quickly iterating over tokens while developing new components.</p>
<h2 id="updates">Updates</h2>
<h3 id="0-9-0-beta-2024-2-20">[0.9.0-beta] - 2024-2-20</h3>
<p>The existing core tokens will still live in <code>./src/lib/design-tokens</code> but at a future time we’ll migrate those over to thie official lib itself for component work.</p>
<p>This folder will contain a config specifically for building tokens to help with the grid/container layout work which actually contains many interdependent tokens and their related scales.</p>
<h2 id="naming-convention-guidelines">Naming Convention Guidelines</h2>
<p>These guidelines are specific to component tokens and not core ones. Not every component needs a full array of predefined tokens. Whenever possible we should aim to alias all token values to core tokens.</p>
<blockquote>
<p><strong>NOTE:</strong> These guidelines are a work in progress and will continue to evolve as DesignOps explores what works in our 1.0.0 quality component releases, and we recieve feedback from our consuming development teams.</p>
</blockquote>
<h3 id="example">Example</h3>
<pre class="language-scss">{@html `<code class="language-scss"><span class="hljs-selector-class">.m-component</span> {
	<span class="hljs-comment">/* A simple example, WIP */</span>
	<span class="hljs-comment">/* This isn&#x27;t a hard rule, but a solid guideline for consistency */</span>

	<span class="hljs-comment">// Recommended convention</span>
	<span class="hljs-comment">// --m-{component}-{property}-{scale}</span>
	<span class="hljs-attr">--m-component-gap</span>: <span class="hljs-built_in">var</span>(--size-gap-default);

	<span class="hljs-comment">// Default styles</span>
	<span class="hljs-attribute">display</span>: flex;
	<span class="hljs-attribute">gap</span>: <span class="hljs-built_in">var</span>(--m-component-gap);

	<span class="hljs-comment">// This is an ovveride for a specific variant based on class</span>
	<span class="hljs-comment">// We override the component token value by applying a new core token alias to the component token</span>
	<span class="hljs-comment">// This practice will also help us inform our API best practices for CSS and future JavaScript components</span>
	&amp;<span class="hljs-selector-class">.is-variant-large</span> {
		<span class="hljs-attr">--m-component-gap</span>: <span class="hljs-built_in">var</span>(--size-gap-l);
	}
}</code>`}</pre>
</Layout_MDSVEX_DEFAULT>