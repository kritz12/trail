<!--
 @component
A component for setting UI Headings
-->
<script>
/** @type {	'h1' | 'h2' |'h3' |'h4' | 'h5' | 'h6' } */
export let as = 'h1';

/** @type {string | null}  */
export let id = null;

/** @type {import('../index').MFontSizes} */
export let size = 'm';

/** @type {import('../index').MTextVariants} */
export let color = 'text';

export let truncate = false;

/** @type {import('../index').MSizeTextMeasures} */
// export let measure = null;
// TODO: @jgibson remap measure API from numbers to tshirt sizes
</script>

<svelte:element this={as} {id} class={`m-heading is-size-${size} is-${color}`} class:truncate>
	<slot />
</svelte:element>
