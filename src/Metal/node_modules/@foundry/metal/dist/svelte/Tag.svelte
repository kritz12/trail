<!--
@component
A Tag Component

*Deprecated*
-->
<script>
	/** @type { 'blue' | 'cyan' | 'green' | 'red' | 'yellow' | null }  */
	export let type = 'blue';
	let typeClass = type ? `is-${type}` : '';

	/** @type boolean */
	export let disabled = false;

	/** @type boolean */
	export let outline = false;

	/** @type boolean */
	export let rounded = false;
	
	
</script>

<div
	class={`m-tag ${typeClass}`}
	class:is-disabled={disabled}
	class:is-outline={outline}
	class:is-rounded={rounded}
>
	<slot/>
</div>
