<!--
 @component
 A versatile code component for sizing and styling UI typography
-->
<script>
/** @type {'code' | 'p' | 'span' | 'div'} as */
export let as = 'code';

/** @type {import('../index').MFontSizes} */
export let size = 'm';
</script>

<svelte:element this={as} class={`m-code is-size-${size}`}>
	<slot />
</svelte:element>
