<!--
 @component
 Help text is pertinent information that assists the user in correctly completing a field.
-->
<script>
/** @type {import('../index').MColorAllTextStatuses} */
export let status = 'neutral';
</script>

<span class={`m-help-text ${status !== 'neutral' ? `is-${status}` : ''}`}>
	{#if $$slots.icon}
		<div class="m-button-icon">
			<slot name="icon" />
		</div>
	{/if}

	<slot />
</span>
